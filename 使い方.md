# AI秘書の使い方

このAI秘書は、あなたの「もう1人の自分」です。
**普通に話しかけるだけ**で、タスク管理・段取り・相談・文書作成・会議メモの整理をやってくれます。

> コマンド（`/morning` など）を覚えなくても、自然な言葉で頼めば同じことができます。コマンドは「いつものやつを一発で呼ぶショートカット」だと思ってください。
>
> 📖 このガイドはいつでも **`/使い方`**（または「使い方教えて」）で表示できます。

---

## まず、これだけ覚えれば動く

| やりたいこと | こう言う（またはコマンド） |
|---|---|
| 今日何やればいい？ | 「おはよう」「今日の予定は？」 / `/morning` |
| タスクを足す | 「〇〇ってタスク追加して、来週金曜まで」 / `/task add "..."` |
| タスク終わった | 「〇〇終わった」 / `/task done <ID>` |
| 相談したい | 「この進め方どう思う？」 / `/ask "..."` |
| 文章を書いてほしい | 「〇〇さんへのメール書いて」 / `/draft ...` |
| 会議メモを取り込む | 「このVTT取り込んで」（ファイルを渡す） / `/extract <file>` |

迷ったら、**やりたいことをそのまま日本語で言えばOK**です。

---

## 1日の流れ（おすすめの使い方）

### 🌅 朝：まず「おはよう」
```
おはよう
```
→ 今日の会議・期限が近いタスク・やるべきこと・放置タスクの棚卸しを、優先順位つきで出します。
「昨日の〇〇会議、何か対応ある？」と事後フォローも聞いてきます。

### ☀️ 日中：思いついたら都度ぶっこむ
- 「〇〇さんから△△頼まれた、タスクにして」→ 背景・期限・担当を確認して登録
- 「今日どれからやるべき？」→ 隙間時間に合わせて並べ替え（`/priorities`）
- 「この報告書の段取り組んで」→ 締切から逆算してマイルストーン設計（`/plan`）

### 🧑‍🤝‍🧑 会議の後：メモ／VTTを渡す
```
このVTT取り込んで（ファイルを添付 or パスを指定）
```
→ タスク・決定事項・新しい関係者を抽出 → **「表と実態にズレない？」「行間で気になることは？」と確認** → あなたの承認後にデータへ反映。
抽出結果に「それ違うよ」と言えば、その場で直せます（会議の中身を見ながら会話できます）。

### 📝 週末／報告前：週報と棚卸し
```
今週の週報まとめて
```
→ 会議トランスクリプト・メモ・下書き、どんな形でも上司向けの週報に整形（`/report`）。

---

## 4つの「モード」（自動で切り替わります）

意識しなくても、話の内容で勝手に切り替わります。頭の片隅に置いておくと便利です。

| モード | いつ | 何をする |
|---|---|---|
| 🗂 秘書（通常） | タスク・予定・段取り | 「いつ・何をやるか」を管理・提案 |
| 💭 相談 | 「どうすべき？」「なぜ？」 | 深く掘って、選択肢・立ち回り・リスクを分析（`/ask`） |
| ✍️ 執筆 | 「書いて」 | メール・報告書・議事録のドラフト（`/draft`） |
| 📥 抽出 | 「読み取って」 | VTT・メモからタスクや決定事項を抜く（`/extract`） |

> 相談・執筆・抽出は**同じ会話の中**で動くので、出てきた結果に対してそのまま「ここ直して」「もっとこうして」と続けられます。

---

## コマンド一覧（全部覚えなくてOK）

```
/使い方        この使い方ガイドを表示
/morning       朝のブリーフィング
/priorities    今日の優先順位
/task          タスク add / done / update / drop / list / search
/plan <ID>     段取り・マイルストーン設計
/draft <対象>  ドラフト作成（執筆モード）
/ask "..."     深い相談（相談モード）
/extract <file> VTT・メモから抽出（抽出モード）
/report        週報生成
/review-fb     レビューFBを記録（議事録取込・FB貼付から自動でも起動）
/learn         学習状況・検出パターンの確認
/meeting       会議の一覧/追加/変更/キャンセル/週次リセット
/setup         初期セットアップ / PJ追加 / 情報更新
/healthcheck   データ全体の整合性チェック
/dashboard     全タスクのダッシュボードを再生成（ダッシュボード.md）
/who <名前>    関係者のクイックブリーフ（立場・力関係・レビュー傾向）
```

---

## 知っておくと安心なこと

- **勝手に書き換えません。** タスク登録もデータ更新も、必ずあなたの承認を取ってから実行します。
- **提案には必ず根拠がつきます。**「期限が3日後なので」「〇〇さんは数字を重視するので」など。
- **裏で学習しています。** あなたが提案を直したり条件を指定したりすると、そっと記録して次回に活かします（通知はしません）。`/learn` で何を学んだか見られます。
- **レビューFBは自動で拾います。** 議事録の取り込み中や、もらった指摘を貼り付けたときに「これFBですね、記録しますか？」と提案します。レビュアーの傾向を学習し、次の `self-review`（提出前チェック）に活かします。`/review-fb` で手動起動も可。
- **会議・レビュー・面談はタスクにしません。** 親タスクの通過点（マイルストーン）として扱い、一覧がレビューだらけになるのを防ぎます。
- **他人担当のタスクは「ウォッチ中」枠に分かれます。** 自分がやることと混ざりません。

---

## 困ったとき

- 表示やデータがおかしい気がする → 「ヘルスチェックして」 / `/healthcheck`
- 何を覚えてる？ → 「学習状況見せて」 / `/learn`
- 新しいPJが始まった → 「PJ追加して」 / `/setup add-project`
- 関係者が異動した・制約が変わった → そのまま伝えれば、影響範囲を調べて反映を提案します

---

データの中身や構造を知りたいときは `README.md`（システム概要）と `.claude/docs/data-structure.md`（データ定義）を見てください。
